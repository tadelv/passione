/**
 * WebSocket server wrapper using the `ws` package.
 *
 * Re-exports the WebSocketServer class from `ws` with the same
 * interface expected by mock-server.js.
 */

export { WebSocketServer } from 'ws'
